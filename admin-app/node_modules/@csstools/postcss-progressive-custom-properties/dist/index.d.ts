import type { PluginCreator } from 'postcss';

interface Options {}

const creator: PluginCreator<Options> = (options) => {
  return (root, result) => {
    // Plugin logic goes here
  };
};

creator.postcss = true;

export default creator;
