import type { PluginCreator } from 'postcss';

export type PluginOptions = {
    color?: string;
    preserve?: boolean;
};

export const pluginCreator: PluginCreator<PluginOptions> = () => ({
    // Your plugin implementation here
});

// Alternatively, if you want to use the creator function directly:
// export default pluginCreator;
