/**
 * Multiplies two matrices or a matrix with a vector.
 * Throws an error if the matrices have incompatible dimensions.
 *
 * @author Lea Verou 2020 MIT License
 * @license W3C https://www.w3.org/Consortium/Legal/2015/copyright-software-and-document
 * @copyright This software or document includes material copied from or derived from https://github.com/w3c/csswg-drafts/blob/main/css-color-4/multiply-matrices.js. Copyright © 2022 W3C® (MIT, ERCIM, Keio, Beihang).
 * @see https://github.com/w3c/csswg-drafts/blob/main/css-color-4/multiply-matrices.js
 *
 * @param a - The first matrix or vector.
 * @param b - The second matrix or vector.
 * @returns The product of the two matrices or the multiplication of the matrix and the vector.
 * @throws {Error} If the matrices have incompatible dimensions.
 */
export declare function multiplyMatrices(a: number[][] | number[], b: number[][] | number[]): number[][] | number[] {
  if (a.length === 0 || (a[0] as number[]).length === 0) {
    throw new Error('The first matrix cannot be empty.');
  }

  if (b.length === 0 || (b[0] as number[]).length === 0) {
    throw new Error('The second matrix cannot be empty.');
  }

  if ((a[0] as number[]).length !== b.length) {
    throw new Error('The matrices have incompatible dimensions.');
  }

  const result = a[0].map((_: number, colIndex: number) => {
    return b.map((row: number[]) => {
      return row.reduce((sum: number, value: number, rowIndex: number) => {
        if ((a[rowIndex + 1]
