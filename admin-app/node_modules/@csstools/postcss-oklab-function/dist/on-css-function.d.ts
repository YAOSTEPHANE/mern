import type { FunctionNode } from 'postcss-value-parser';
import { Declaration, Result } from 'postcss';

/**
 * Handles the 'srgb()' CSS function.
 * @param {FunctionNode} node - The function node to process.
 */
export function onCSSFunctionSRgb(node: FunctionNode): void {
  // Function implementation here
}

/**
 * Handles the 'display-p3()' CSS function.
 * @param {FunctionNode} node - The function node to process.
 * @param {Declaration} decl - The declaration object.
 * @param {Result} result - The result object.
 * @param {boolean} preserve - A flag indicating if the value should be preserved.
 */
export function onCSSFunctionDisplayP3(node: FunctionNode, decl: Declaration, result: Result, preserve: boolean): void {
  // Function implementation here
}
