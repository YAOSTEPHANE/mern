import { PluginCreator } from 'postcss';

type SubFeatures = {
  displayP3?: boolean;
};

type PluginOptions = {
  enableProgressiveCustomProperties?: boolean;
  preserve?: boolean;
  subFeatures?: SubFeatures;
};

/**
 * Transforms `oklab()` and `oklch()` functions in CSS.
 */
const postcssOklchTransformPlugin: PluginCreator<PluginOptions> = (options) => {
  // plugin implementation here
};

export default postcssOklchTransformPlugin;
