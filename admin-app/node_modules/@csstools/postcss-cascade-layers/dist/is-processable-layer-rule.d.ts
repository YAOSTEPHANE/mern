import type { AtRule } from 'postcss';

/**
 * Checks if the given AtRule is a processable layer rule.
 * @param atRule - The AtRule to check.
 * @returns True if the AtRule is a processable layer rule, false otherwise.
 */
export declare function isProcessableLayerRule(atRule: AtRule): boolean;
