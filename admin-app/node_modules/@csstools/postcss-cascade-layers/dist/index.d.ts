import type { PluginCreator } from 'postcss';

interface PluginOptions {
  // add any options schema here
}

const pluginOptions = {};

const creator: PluginCreator<PluginOptions> = (options) => {
  return (root, result) => {
    // add your plugin implementation here
  };
};

export default creator;
