import { PluginCreator } from 'postcss';

type PluginOptions = {
  preserve?: boolean;
  onComplexSelector?: 'warning' | 'error';
  onPseudoElement?: 'warning' | 'error';
  specificityMatchingName?: string;
};

const creator: PluginCreator<PluginOptions> = (options) => {
  // Implement the plugin logic here
  return () => {};
};

export default creator;
