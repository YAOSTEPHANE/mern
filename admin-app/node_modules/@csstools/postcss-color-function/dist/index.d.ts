import { PluginCreator } from 'postcss';

export interface PluginOptions {
  preserve?: boolean;
  enableProgressiveCustomProperties?: boolean;
}

export const postcssPlugin: PluginCreator<PluginOptions> = (options) => {
  return (root, result) => {
    // Your plugin logic here
  };
};
