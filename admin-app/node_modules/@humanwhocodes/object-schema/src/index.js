/**
 * @fileoverview Object Schema Package
 */

// Use destructuring to import the required modules
const { ObjectSchema } = require("./object-schema");
const { MergeStrategy } = require("./merge-strategy");
const { ValidationStrategy } = require("./validation-strategy");

// Export all the required modules as default exports
module.exports = {
  ObjectSchema,
  MergeStrategy,
  ValidationStrategy,
};
