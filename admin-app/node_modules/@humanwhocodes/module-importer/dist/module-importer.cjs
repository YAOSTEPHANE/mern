'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

const module_1 = require('module');
const url = require('url');
const path = require('path');

/**
 * @fileoverview Universal module importer
 */

//-----------------------------------------------------------------------------
// Helpers
//-----------------------------------------------------------------------------

const __filename = url.fileURLToPath(typeof document === 'undefined' ? require('u' + 'rl').URL.fileURLToPath(import.meta.url) : url.pathToFileURL(document.currentScript.src));
const __dirname = path.dirname(__filename);
const require$1 = module_1.createRequire(path.join(__dirname, 'module-importer.cjs'));
const { ModuleImporter } = require$1("./module-importer.cjs");

exports.ModuleImporter = ModuleImporter;

