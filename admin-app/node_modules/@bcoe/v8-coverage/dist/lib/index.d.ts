import * as ascii from "./ascii";
import * as clone from "./clone";
import * as compare from "./compare";
import * as merge from "./merge";
import { RangeTree } from "./range-tree";
import { ProcessCov, ScriptCov, FunctionCov, RangeCov } from "./types";

const exports = {
  ascii: {
    emitForest: ascii.emitForest,
    emitForestLines: ascii.emitForestLines,
    parseFunctionRanges: ascii.parseFunctionRanges,
    parseOffsets: ascii.parseOffsets,
  },
  clone: {
    cloneFunctionCov: clone.cloneFunctionCov,
    cloneProcessCov: clone.cloneProcessCov,
    cloneScriptCov: clone.cloneScriptCov,
    cloneRangeCov: clone.cloneRangeCov,
  },
  compare: {
    compareScriptCovs: compare.compareScriptCovs,
    compareFunctionCovs: compare.compareFunctionCovs,
    compareRangeCovs: compare.compareRangeCovs,
  },
  merge: {
    mergeFunctionCovs: merge.mergeFunctionCovs,
    mergeProcessCovs: merge.mergeProcessCovs,
    mergeScriptCovs: merge.mergeScriptCovs,
  },
  RangeTree,
  ProcessCov,
  ScriptCov,
  FunctionCov,
  RangeCov,
};

export default exports;
