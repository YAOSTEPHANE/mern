"use strict";

Object.defineProperty(exports, "__esModule", { value: true });
exports.resolveFSPath = exports.default = _default;

function _default(moduleName, dirname, absoluteRuntime) {
  if (absoluteRuntime === false) return moduleName;
  if (absoluteRuntime) {
    resolveFSPath();
  }
}

function resolveFSPath() {
  if (typeof window === "undefined" || !window.document) {
    throw new Error(
      "The 'absoluteRuntime' option is not supported when using @babel/standalone in a non-browser environment."
    );
  }
  throw new Error("The 'absoluteRuntime' option is not supported when using @babel/standalone.");
}
