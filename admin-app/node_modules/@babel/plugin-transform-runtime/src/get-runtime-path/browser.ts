export function getModuleName(moduleName: string, dirname: string, absoluteRuntime: string | boolean): string {
  if (absoluteRuntime === false) {
    return moduleName;
  }

  // Throw an error if absoluteRuntime is not false
  throw new Error(
    "The 'absoluteRuntime' option is not supported when using @babel/standalone."
  );
}
