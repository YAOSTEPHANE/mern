"use strict";

const { declare } = require("@babel/helper-plugin-utils");
const exportNamespaceFrom = require("babel-plugin-export-namespace-from");

module.exports = declare((api) => {
  api.assertVersion(7);

  return {
    name: "syntax-export-namespace-from",

    manipulateOptions(opts, parserOpts) {
      parserOpts.plugins.push("exportNamespaceFrom");
    },

    visitor: {
      // Include this visitor if you want to do something with the parsed AST
      // For example, you can use it to add a new top-level namespace export
      Program(path) {
        // Add your logic here
      }
    }
  };
});
