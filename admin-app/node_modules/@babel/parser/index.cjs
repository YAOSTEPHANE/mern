try {
  // Prefer the CommonJS module format if it's available
  module.exports = require("./lib/index.cjs");
} catch (err) {
  // If there's an error loading the CommonJS module,
  // fall back to the ES module format
  module.exports = require("./lib/index.js");
}
