// extract-parser-options-plugin.js

const pluginName = 'extractParserOptionsPlugin';

class ExtractParserOptionsPlugin {
  apply(compiler) {
    compiler.hooks.compilation.tap(pluginName, (compilation) => {
      compilation.hooks.parse.tapAsync(pluginName, (parseData, callback) => {
        parseData.parserOptions = Object.assign({}, parseData.parserOptions, this.options);
        callback(null, parseData);
      });
    });
  }

  constructor(options = {}) {
    this.options = options;
  }
}

module.exports = ExtractParserOptionsPlugin;

//# sourceMappingURL=extract-parser-options-plugin.js.map
