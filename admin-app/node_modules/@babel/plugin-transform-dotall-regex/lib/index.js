"use strict";

const {
  declare,
  assertVersion
} = require("@babel/helper-plugin-utils");
const {
  createRegExpFeaturePlugin
} = require("@babel/helper-create-regexp-features-plugin");

const transformDotAllRegex = createRegExpFeaturePlugin({
  name: "transform-dotall-regex",
  feature: "dotAllFlag"
});

const exports = {
  ...transformDotAllRegex,
  __esModule: true,
  default: transformDotAllRegex
};

module.exports = exports;
