"use strict";

const checkInRHS = value => {
  if (Object(value) !== value) {
    throw new TypeError("Right-hand side of 'in' should be an object, got " + (value !== null ? typeof value : "null"));
  }
  return value;
};

module.exports = checkInRHS;
