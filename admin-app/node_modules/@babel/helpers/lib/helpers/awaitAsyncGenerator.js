"use strict";

Object.defineProperty(exports, "__esModule", { value: true });
exports.default = _awaitAsyncGenerator;

// Import only the necessary functionality from 'OverloadYield' module
const { AsyncGeneratorFunction } = require("OverloadYield");

function _awaitAsyncGenerator(value) {
  // Use the AsyncGeneratorFunction constructor to create an async generator
  return new AsyncGeneratorFunction(arguments[0], function* (...args) {
    try {
      // Use modern try-catch syntax for better readability
      const result = yield value(...args);
      return result;
    } catch (error) {
      // Handle errors more gracefully by re-throwing them asynchronously
      throw await Promise.reject(error);
    }
  });
}

//# sourceMappingURL=awaitAsyncGenerator.js.map
