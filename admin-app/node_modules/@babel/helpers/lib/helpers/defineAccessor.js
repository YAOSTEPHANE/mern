"use strict";

Object.defineProperty(exports, "__esModule", { value: true });
exports.defineAccessor = void 0;

/**
 * Defines an accessor property on an object with the specified type, obj, key, and function.
 * @param {string} type - The type of property to define, either 'get' or 'set'.
 * @param {object} obj - The object to define the property on.
 * @param {string} key - The name of the property to define.
 * @param {Function} fn - The function to use as the property value.
 * @returns {object} The result of defining the property on the object.
 */
function defineAccessor(type, obj, key, fn) {
  const descriptor = {
    configurable: true,
    enumerable: true,
  };

  // Use the specified type to define either a getter or setter function
  descriptor[type] = fn;

  return Object.defineProperty(obj, key, descriptor);
}

exports.defineAccessor = defineAccessor;
