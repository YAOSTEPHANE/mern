"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = setFunctionName;

/**
 * Sets the name of a function with a given prefix.
 * @param {Function} fn - The function to set the name of.
 * @param {string|symbol} name - The name to set for the function. If it's a symbol, its description will be used.
 * @param {string} [prefix] - The prefix to prepend to the name.
 * @returns {Function} The function with the updated name.
 */
function setFunctionName(fn, name, prefix) {
  if (typeof name === "symbol") {
    name = name.description || "";
  }

  if (typeof name !== "string") {
    console.error("Invalid name provided. Expected a string or a symbol.");
    return fn;
  }

  try {
    Object.defineProperty(fn, "name", {
      configurable: true,
      value: prefix ? `${prefix} ${name}` : name
    });
  } catch (error) {
    console.warn("Failed to set the function name:", error);
  }

  return fn;
}

//# sourceMappingURL=setFunctionName.js.map
