/**
 * Builds a function that checks if a member matches a given dotted path.
 * @param match - The dotted path to match.
 * @param allowPartial - Whether to allow partial matches.
 * @returns A function that takes a member and returns a boolean indicating if it matches.
 */
function buildMatchMemberExpression(match: string, allowPartial: boolean) {
  const parts = match.split(".");
  return (member: object) => (0, _matchesPattern.default)(member, parts, allowPartial);
}

/**
 * The default export of the module.
 */
const { default: _matchesPattern } = require("./matchesPattern.js");

//# sourceMappingURL=buildMatchMemberExpression.js.map

export default buildMatchMemberExpression;
