"use strict";

import { isVariableDeclaration } from "./generated/index.js";
import { BLOCK_SCOPED_SYMBOL } from "../constants/index.js";

// Function to check if a node is a variable declaration and not block-scoped
function isVar(node) {
  return (
    isVariableDeclaration(node, { kind: "var" }) &&
    !node[BLOCK_SCOPED_SYMBOL]
  );
}

// Export the isVar function as the default export
export default isVar;

// Source map for the code
//# sourceMappingURL=isVar.js.map
