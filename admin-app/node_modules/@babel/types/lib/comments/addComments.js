"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.addComments = void 0;

/**
 * Add comments to a node with the given type.
 * @param {Node} node - The node to add comments to.
 * @param {string} type - The type of comments to add. Can be "leading" or "trailing".
 * @param {Comment[]} comments - The comments to add.
 * @returns {Node} The modified node with the added comments.
 */
function addComments(node, type, comments) {
  if (!comments || !node) return node;

  const commentsKey = `${type}Comments`;
  const existingComments = node[commentsKey];

  if (existingComments) {
    comments = (type === "leading") ? existingComments.concat(comments) : existingComments.concat(comments);
  }

  node[commentsKey] = comments;
  return node;
}

exports.addComments = addComments;
