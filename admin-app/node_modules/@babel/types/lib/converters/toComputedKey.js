"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = toComputedKey;

// Import only the required functions from the validators and builders modules
// to avoid unnecessary imports and improve performance
const { isIdentifier } = require("../validators/generated");
const { stringLiteral } = require("../builders/generated");

function toComputedKey(node, key = node.key || node.property) {
  // Check if the node has a computed property and if the key is an identifier
  if (!node.computed && isIdentifier(key)) {
    // Return a string literal using the name of the identifier
    return stringLiteral(key.name);
  }

  // Return the key as is if it's not an identifier or if the node has a computed property
  return key;
}

//# sourceMappingURL=toComputedKey.js.map

