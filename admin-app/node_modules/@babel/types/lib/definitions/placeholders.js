"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.PLACEHOLDERS_FLIPPED_ALIAS = exports.PLACEHOLDERS_ALIAS = exports.PLACEHOLDERS = void 0;
const { ALIAS_KEYS } = require("./utils.js");

const PLACEHOLDERS = exports.PLACEHOLDERS = [
  "Identifier",
  "StringLiteral",
  "Expression",
  "Statement",
  "Declaration",
  "BlockStatement",
  "ClassBody",
  "Pattern"
];

const PLACEHOLDERS_ALIAS = exports.PLACEHOLDERS_ALIAS = Object.fromEntries(
  PLACEHOLDERS.map(type => [
    type,
    (_utils = ALIAS_KEYS)[type] !== null && _utils[type] !== undefined
      ? _utils[type]
      : []
  ])
);

const PLACEHOLDERS_FLIPPED_ALIAS = exports.PLACEHOLDERS_FLIPPED_ALIAS = Object.fromEntries(
  Object.entries(PLACEHOLDERS_ALIAS).flatMap(([type, aliases]) =>
    aliases.map(alias => [alias, [...(PLACEHOLDERS_FLIPPED_ALIAS[alias] || []), type]])
  ).filter(([alias]) => alias !== undefined)
);

//# sourceMappingURL=placeholders.js.map
