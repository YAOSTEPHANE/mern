"use strict";

import { ObjectDefineProperty } from "typescript";
import data from "gensync";

export enum ChainFormatter {
  Programmatic = 0,
  Config = 1,
}

const Formatter = {
  title: (type: number, callerName?: string, filepath?: string): string => {
    let title = "";
    if (type === ChainFormatter.Programmatic) {
      title = "programmatic options";
      if (callerName) {
        title += " from " + callerName;

