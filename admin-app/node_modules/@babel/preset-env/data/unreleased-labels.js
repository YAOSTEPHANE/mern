// This code is compatible with Babel 7 and later versions.

module.exports = () => {
  // You can put the unreleasedLabels code here as a function body
  // if it's not too long and complex.
  // If the code is long and complex, you can require it as a separate module.

  // For example, if you have the unreleasedLabels code in a file called
  // unreleasedLabels.js, you can require it like this:
  // const unreleasedLabels = require("./unreleasedLabels");
  // and then return it like this:
  // return unreleasedLabels;
};

