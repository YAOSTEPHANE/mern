// TypeScript definition for better type checking
import type { PluginItem } from "@babel/core";

// Use the latest version of the compat data plugins
const { plugins } = require("@babel/compat-data/plugins");

// Export the plugins as the default export
module.exports = plugins as PluginItem[];
