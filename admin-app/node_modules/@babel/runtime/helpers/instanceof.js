// Defining the _instanceof function
const _instanceof = (left, right) => {
  if (right != null && typeof Symbol !== "undefined" && right[Symbol.hasInstance]) {
    return !!right[Symbol.hasInstance](left);
  } else {
    return left instanceof right;
  }
};

// Exporting the function as the default export
module.exports = {
  // Adding __esModule property
  __esModule: true,
  // Setting the default export to _instanceof
  default: _instanceof,
};
