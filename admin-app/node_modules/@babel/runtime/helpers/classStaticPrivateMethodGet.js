// Import the required module
const classCheckPrivateStaticAccess = require('./classCheckPrivateStaticAccess.js');

// Export the default function
module.exports = {
  _classStaticPrivateMethodGet: (receiver, classConstructor, method) => {
    // Check if the receiver is an instance of the classConstructor
    classCheckPrivateStaticAccess(receiver, classConstructor);

    // Return the method
    return method;
  }
};

// Set module.exports as an ES6 module
module.exports.__esModule = true;

// Since we are using the default export, we don't need to set module.exports["default"]
