const _classPrivateFieldBase = (receiver, privateKey) => {
  if (receiver == null || !Object.prototype.hasOwnProperty.call(receiver, privateKey)) {
    throw new TypeError("attempted to use private field on non-instance");
  }
  return receiver;
};

export default _classPrivateFieldBase;
