export default function nonIterableSpread(instance) {
  if (!Symbol.iterator in Object(instance)) {
    throw new TypeError(
      `Invalid attempt to spread ${instance}.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.`
    );
  }
  // Add any additional logic or functionality here if needed
}
