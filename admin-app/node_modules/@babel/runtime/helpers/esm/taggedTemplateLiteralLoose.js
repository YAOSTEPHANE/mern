export default function taggedTemplateLiteral(strings, ...values) {
  const raw = strings.raw;
  const result = strings.reduce((acc, str, i) => {
    acc.push(str);
    if (values[i]) {
      acc.push(values[i]);
    }
    return acc;
  }, []);
  result.raw = raw;
  return result;
}
