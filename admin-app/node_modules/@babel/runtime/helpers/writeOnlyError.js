// Defining the write-only error function
function writeOnlyError(name) {
  throw new TypeError(`"${name}" is write-only`);
}

// Exporting the function as the default export and as a named export
module.exports = writeOnlyError;
module.exports.default = writeOnlyError;
module.exports.__esModule = true;
