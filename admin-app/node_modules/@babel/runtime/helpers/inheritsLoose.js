// Importing only the necessary 'setPrototypeOf' function from 'setPrototypeOf.js'
const { setPrototypeOf } = require("./setPrototypeOf.js");

// Defining the _inheritsLoose function
function _inheritsLoose(subClass, superClass) {
  // Setting the prototype of the subClass to an object created from the superClass's prototype
  subClass.prototype = Object.create(superClass.prototype);

  // Setting the constructor property of the subClass prototype to the subClass constructor
  subClass.prototype.constructor = subClass;

  // Setting the prototype of the subClass to the superClass
  setPrototypeOf(subClass, superClass);
}

// Exporting the _inheritsLoose function as the default export
module.exports = _inheritsLoose;

// Setting __esModule to true
module.exports.__esModule = true;

// Removing the unnecessary line of code that sets the default export to the module.exports object
// as it is already set by the line above
// module.exports["default"] = module.exports;
