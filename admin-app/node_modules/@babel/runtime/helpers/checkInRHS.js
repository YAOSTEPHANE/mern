// Add proper export statement for _typeof function
module.exports = {
  _typeof: function(obj) {
    return typeof obj;
  }
};


const { _typeof } = require('./typeof.js');

// Rename _checkInRHS to checkInRHS for better readability
function checkInRHS(e) {
  if (Object(e) !== e) {
    // Use template literals for better string interpolation
    throw new TypeError(`Right-hand side of 'in' should be an object, got ${_typeof(e) || 'null'}`);
  }
  // Return e without wrapping it in an Object, as it is not necessary
  return e;
}

// Export the function as default for easier import
module.exports = checkInRHS;
module.exports.default = checkInRHS;
module.exports.__esModule = true;
