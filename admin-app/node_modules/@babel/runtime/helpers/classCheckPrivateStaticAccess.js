// Define the class check private static access function
function classCheckPrivateStaticAccess(receiver, classConstructor) {
  if (receiver !== classConstructor) {
    throw new TypeError("Private static access of wrong provenance");
  }
}

// Export the function as the default export
module.exports = classCheckPrivateStaticAccess;

// Set the module to be an ES module
module.exports.__esModule = true;

// Since there's only one export, there's no need to create a named export
// for the default export, so we can remove the following line:
// module.exports["default"] = module.exports;
