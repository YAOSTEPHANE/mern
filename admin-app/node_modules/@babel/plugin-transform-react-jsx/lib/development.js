"use strict";

const { createPlugin } = require('./create-plugin');

const developmentPlugin = name => ({
  development: true,
  name,
});

const defaultPlugin = createPlugin(developmentPlugin('transform-react-jsx'));

module.exports = defaultPlugin;
