/**
Create a type that represents either the value or the value wrapped in `Promise<T>`.

Use-cases:
- A function accepts a callback that may either return a value synchronously or may return a promised value.
- This type could be the return type of `Promise<T>.then()`, `Promise<T>.catch()`, and `Promise<T>.finally()` callbacks.

Please upvote [this issue](https://github.com/microsoft/TypeScript/issues/313
